# Test dependencies for HiveFW Home Assistant integration.
#
# Recommended:
#   uv venv --python 3.14 .venv
#   uv pip install -r requirements_test.txt
#   .venv/bin/python -m pytest tests/ -v
#
# pytest-homeassistant-custom-component pulls the Home Assistant test surface
# used by the backend tests.

pytest-homeassistant-custom-component

# Integration runtime dependencies required during test collection.
cachetools>=5.3.0
meshcore>=2.3.7
pycryptodome>=3.20.0
paho-mqtt>=1.6.0
pynacl>=1.5.0
